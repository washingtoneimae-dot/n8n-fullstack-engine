{
  "name": "gateway-prototype",
  "version": "0.0.1",
  "private": true,
  "description": "Proof-of-concept: forwards a browser request to an n8n webhook and renders the JSON response as an HTML page.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
